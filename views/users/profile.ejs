<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <style>
        /* Button styles */
.btn {
    padding: 10px 20px;              /* Padding around the button */
    border: none;                    /* Remove default border */
    border-radius: 5px;             /* Rounded corners */
    font-size: 16px;                /* Font size */
    cursor: pointer;                 /* Pointer cursor on hover */
    transition: background-color 0.3s, transform 0.3s; /* Smooth transition */
    margin: 5px;      
                  /* Margin for spacing between buttons */
}

/* Edit Profile button */
.edit-profile-button {
    background-color: #007BFF;      /* Primary color */
    color: white;                    /* Text color */
}

/* Hover effect for Edit Profile button */
.edit-profile-button:hover {
    background-color: #0056b3;      /* Darker blue on hover */
    transform: translateY(-2px);    /* Slight lift on hover */
}

/* Delete Posts button */
.delete-btn {
    background-color: #28a745;      /* Green color */
    color: white;                   /* Text color */
}

/* Hover effect for Delete button */
.delete-btn:hover {
    background-color: #218838;      /* Darker green on hover */
    transform: translateY(-2px);    /* Slight lift on hover */
}

/* Logout button */
.logout-btn {
    background-color: #dc3545;      /* Red color */
    color: white;                   /* Text color */
}

/* Hover effect for Logout button */
.logout-btn:hover {
    background-color: #c82333;      /* Darker red on hover */
    transform: translateY(-2px);    /* Slight lift on hover */
}

/* Additional styling for buttons within profile-buttons class */
.profile-buttons {
    display: flex;
    justify-content: center;   /* Spacing between buttons */        
    margin-bottom: 20px;           /* Space below the profile details */                 
}

    </style>
</head>
<body>
    <%- include('../layouts/header.ejs') %>    
   
     
    <header>
        <h1>Welcome to your profile : <%= user.username  %></h1>
    </header>
    <div class="profile-container">
        <div class="profile-header">
            <img src="#"  class="profile-avatar"> <!-- Placeholder for user avatar -->
            <h1><%= user.username %></h1>
            <p><i class="fas fa-envelope"></i> <%= user.email %></p>
        </div>

        <center>
            <div class="profile-buttons">
                <a href="/edit/profile/<%= user.id %>">
                    <button class="btn delete-btn" style="background-color: #0056b3;">Edit profile</button>
                </a>
                <a href="/post/user/<%= user.id %>">
                <button class="btn delete-btn">View Posts</button>

                </a>
                <button class="btn logout-btn">Logout</button>
            </div>
        </center>
        
  
    </div>
</body>
</html>
